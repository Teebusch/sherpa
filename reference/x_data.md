# Define Alpine Component Data

Provides reactive data for an Alpine component.

## Usage

``` r
x_data(data = "{}")
```

## Arguments

- data:

  A JSON string (e.g., `"{ open: false }"`) or a store name. Use `NA` to
  create a data-less component.

## See also

<https://alpinejs.dev/directives/data>
