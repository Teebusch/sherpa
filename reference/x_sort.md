# Alpine.js Sort Directive

Re-order elements by dragging them with your mouse. Requires Plugin
"Sort"

## Usage

``` r
x_sort(group = NULL, handle = NULL)
```

## Arguments

- group:

  Optional group name to allow sorting between multiple lists

- handle:

  Optional selector for a drag handle (e.g., '.drag-handle')
