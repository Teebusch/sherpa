# Access an Alpine Store

Helper to generate the `$store` syntax for Alpine stores.

## Usage

``` r
x_store_ref(store_id, prop = NULL)
```

## Arguments

- store_id:

  Unique ID of the store.

- prop:

  Optional property within the store.

## Value

A string: `$store.store_id.prop`.
