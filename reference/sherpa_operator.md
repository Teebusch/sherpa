# Sherpa Attribute Operator

Appends Sherpa attributes to a tag without needing the !!! operator.

## Usage

``` r
tag %s% ...
```

## Arguments

- tag:

  A shiny tag

- ...:

  Sherpa attributes (e.g., x_data(), x_cloak())
