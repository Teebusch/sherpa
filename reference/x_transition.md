# Apply CSS Transitions

Smoothly transition elements as they enter/leave the DOM.

## Usage

``` r
x_transition(modifiers = NULL)
```

## Arguments

- modifiers:

  Optional modifiers for specific stages (e.g., "enter", "leave",
  "duration.500ms").

## See also

<https://alpinejs.dev/directives/transition>
